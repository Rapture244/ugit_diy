# ==================================================================================================
# Logging configuration for ugit_diy
# ==================================================================================================
# This file contains optional environment variables to customize how logging is configured.
# Copy this file to `.env` and adjust as needed. All variables here are optional.

# --------------------------------------------------------------------------------------------------
# UGIT_LOG_LEVEL — Override the root logger level.
#
# - Applies even when a dictConfig is used.
# - Affects only the root logger (named loggers keep their explicit levels).
# - If not set, defaults to INFO.
#
# Valid options: DEBUG, INFO, WARNING, ERROR, CRITICAL
UGIT_LOG_LEVEL=INFO
